# ProTrader V4 — QA Bug 報告

> 測試日期：2026-06-12  
> 測試方式：靜態程式碼分析 + 邏輯驗證 + 執行時期模擬  
> 測試範圍：全模組（M1～M6）  

---

## 嚴重等級說明

| 等級 | 說明 |
|------|------|
| 🔴 CRITICAL | 功能完全失效，使用者無法使用該功能 |
| 🟠 HIGH | 主要功能行為異常，影響體驗或產生錯誤資料 |
| 🟡 MEDIUM | 部分功能不正確，但不影響核心流程 |
| 🔵 LOW | 視覺/措辭瑕疵，UX 細節問題 |

---

## 🔴 CRITICAL（4 項）

### BUG-001｜產業鏈地圖：切換鏈類型無效
- **模組**：M3 產業鏈地圖
- **重現**：點擊「Memory / HBM」、「CPO 共封裝光學」、「散熱」、「PCB」任一按鈕
- **現象**：畫面永遠顯示 GPU / AI Server 鏈，切換無任何變化
- **根因**：`const chains` 物件只定義了 `GPU_AI` 一組資料，其餘鍵值不存在。`loadChain(key)` 遇到不存在的 key 時 fallback 至 `chains.GPU_AI`，等同按下按鈕完全無效。
- **影響**：4 個切換按鈕全部失效，產業鏈地圖核心差異化功能喪失

---

### BUG-002｜歷史回放：時間軸嚴重超出收盤時間
- **模組**：M6 歷史回放
- **重現**：開始回放後等待播放至結束
- **現象**：回放時間從 09:00 播放至 24:00，遠超台股收盤時間 13:30
- **根因**：
  - `maxTick = 240`，每 tick 4 分鐘
  - 總播放時間 = 9:00 + 240×4min = **25 小時**
  - 正確做法：台股 9:00–13:30 = 270 分鐘 ÷ 4 = **67 ticks**（應設 `maxTick ≈ 68`）
- **時間軸驗證**：

  | tick | 顯示時間 | 狀態 |
  |------|---------|------|
  | 72   | 13:32   | 超出收盤 |
  | 120  | 16:32   | ❌ 嚴重錯誤 |
  | 180  | 20:16   | ❌ 嚴重錯誤 |
  | 240  | 24:00   | ❌ 嚴重錯誤 |

---

### BUG-003｜switchPage() 使用已廢棄的 global `event` 物件
- **模組**：全域導覽
- **重現**：程式邏輯中直接呼叫 `switchPage(id)`（非按鈕點擊）
- **現象**：`event.currentTarget` 取得全域隱含的 `event` 物件，在非事件觸發下為 `undefined`，導致 `TypeError: Cannot read properties of undefined`
- **根因**：
  ```js
  // 第 638 行（有問題的寫法）
  event.currentTarget.classList.add('active');
  // 正確做法應傳入 el 參數：onclick="switchPage('xxx', this)"
  ```
- **備注**：目前在手動點按下可運作（因瀏覽器 legacy 行為），但屬高風險隱患

---

### BUG-004｜K棒型態辨識圖：不是真正的K棒圖
- **模組**：M4 交易策略室
- **重現**：切換到交易策略室頁面
- **現象**：
  - Y 軸顯示 -15 ~ +15 之類的差值，不是實際股價（應為 1000 上下）
  - 長相類似 bar chart，非標準 K棒（無上下影線）
  - 資訊誤導使用者對進出場位的判斷
- **根因**：使用 `type:'bar'`，資料為 `closes[i] - opens[i]`（收盤減開盤差值），Y 軸原點固定在 0，無法反映真實價格

---

## 🟠 HIGH（8 項）

### BUG-005｜回測報告：重複執行後 equityChart Canvas 錯誤
- **模組**：M5 回測實驗室
- **重現**：點擊「執行回測」按鈕 2 次以上
- **現象**：Chart.js 拋出 `"Canvas is already in use"` 警告，舊圖表殘留於畫面，新圖表可能無法正確渲染
- **根因**：`renderBacktestReport()` 每次呼叫都執行 `new Chart(ctx, ...)` 但從未呼叫 `equityChart.destroy()`。全檔 `new Chart()` 共 6 次，`destroy()` 只有 1 次（`replayChart`）

---

### BUG-006｜回測報告：初始化時 Canvas 在隱藏 panel 中渲染
- **模組**：M5 回測實驗室
- **重現**：首次切換至回測實驗室
- **現象**：equityChart、月度熱圖在 `display:none` 的 `#bt-report` panel 中初始化，圖表尺寸計算為 0×0，圖形顯示異常（扁平或空白）
- **根因**：`initBacktest()` 直接呼叫 `renderBacktestReport()`，未等待 panel 顯示

---

### BUG-007｜真實持倉：近止損判斷邏輯錯誤
- **模組**：M6 真實持倉
- **重現**：觀察持倉列表中的「狀態」欄
- **現象**：以 `price < cost × 1.05` 判斷近止損，而非比對實際 `sl` 欄位，導致錯誤標示
- **根因**：
  ```js
  // 錯誤（第 1172 行）
  const nearSL = h.price < h.cost * 1.05;
  // 正確
  const nearSL = h.price <= h.sl * 1.05;
  ```
- **驗證**：
  - 台積電：cost=920, cost×1.05=966, price=1050 → 正常（兩者一致✓）
  - 鴻海：cost=178, cost×1.05=186.9, sl=155, price=162 → bug 邏輯判近止損（但 price > sl，實際無立即風險）
  - 3711：cost=135, cost×1.05=148.5, sl=128, price=155 → bug 邏輯顯示正常，但 price 僅高於 sl 的 21% 應有警示

---

### BUG-008｜指數分時圖：切換週期後樣式擴散污染
- **模組**：M1 戰情中心
- **重現**：點擊「日 / 週 / 月」切換按鈕
- **現象**：頁面上**所有** `.card .btn-ghost` 按鈕的 background 樣式被清空，包含其他 Card 裡完全無關的按鈕
- **根因**：
  ```js
  // 第 769 行（作用域過廣）
  document.querySelectorAll('.card .btn-ghost').forEach(b => b.style.background='');
  ```

---

### BUG-009｜策略卡切換股票：K棒圖不更新
- **模組**：M4 交易策略室
- **重現**：從下拉選單切換股票（如從台積電換為聯電）
- **現象**：策略卡內容正確更新，但 K棒圖永遠顯示初始隨機資料，不隨股票切換
- **根因**：`loadStrategyCard()` 只更新 strategy grid，未重新呼叫 `initKbar()` 或傳入股票代碼更新圖表

---

### BUG-010｜歷史回放：開啟下單面板後狀態 Badge 不更新
- **模組**：M6 歷史回放
- **重現**：回放中點擊「📝 下單」按鈕
- **現象**：回放實際已暫停，但右上角 Badge 仍顯示紅色「PLAYING」（應改為黃色「PAUSED」）
- **根因**：`openReplayOrder()` 僅設定 `replayState.paused = true`，未更新 Badge DOM

---

### BUG-011｜歷史回放：取消下單面板後強制恢復回放
- **模組**：M6 歷史回放
- **重現**：① 手動暫停回放 → ② 點擊「📝 下單」 → ③ 點擊「取消」
- **現象**：原本手動暫停的狀態被覆蓋，回放意外自動繼續
- **根因**：`closeReplayOrder()` 無條件執行 `replayState.paused = false`，未記錄進入下單前的暫停狀態

---

### BUG-012｜指數分時圖：切換週期後 X 軸 Label 變數字索引
- **模組**：M1 戰情中心
- **重現**：點擊「週」或「月」切換按鈕
- **現象**：X 軸從「09:00, 09:15...」時間格式變為「0, 1, 2...」數字索引，無任何時間意義
- **根因**：
  ```js
  // 第 776 行
  indexChart.data.labels = Array.from({length:n}, (_, i) => i); // ← 只有數字
  ```

---

## 🟡 MEDIUM（9 項）

### BUG-013｜持股快報：「2 警示」數量與實際不符
- **模組**：M1 戰情中心
- **現象**：Header Badge 顯示「2 警示」，但 `renderHoldings()` 只有 1 筆（鴻海）觸發 `badge-red`
- **根因**：Header Badge 硬編碼為 2，未動態計算

---

### BUG-014｜多個按鈕點擊無反應（缺少 onclick）

| 按鈕文字 | 所在模組 | 問題 |
|---------|---------|------|
| `+ 新增` | M6 觀察清單 | 無 onclick |
| `+ 新增持倉` | M6 真實持倉 | 無 onclick |
| `設定警示` | M6 觸價警示 | 無 onclick |
| `取消`（虛擬下單） | M6 虛擬建倉 | 無 onclick |

---

### BUG-015｜虛擬下單：確認訊息股數永遠顯示「1張」
- **模組**：M6 虛擬建倉
- **重現**：在「股數（張）」輸入 5，點擊確認下單
- **現象**：Alert 訊息仍顯示「1張」
- **根因**：`placePaperOrder()` 未讀取 `#paper-shares` 欄位，訊息內容硬編碼

---

### BUG-016｜虛擬建倉帳戶數字靜態不更新
- **模組**：M6 虛擬建倉
- **現象**：下單後「可用現金」、「總市值」、「報酬率」四欄數字永遠維持初始值
- **根因**：帳戶 summary 卡片為純靜態 HTML，`placePaperOrder()` 未觸發任何更新邏輯

---

### BUG-017｜策略卡缺少「壓力位」欄位
- **模組**：M4 交易策略室
- **現象**：策略卡每張只有 6 格（無壓力位），Spec M4 明確要求 `resistance_levels` 欄位
- **根因**：`strategyCards` 資料只定義支撐位，遺漏壓力位

---

### BUG-018｜歷史回放：切換 Tab 後 Interval 持續洩漏
- **模組**：M6 歷史回放
- **重現**：開始回放後切換至「觀察清單」或其他 Tab
- **現象**：回放的 `setInterval` 在背景持續執行，消耗效能且 `replayChart.update()` 在不可見元素上持續呼叫
- **根因**：`switchInner()` 切換內部 Tab 時未執行 `clearInterval(replayState.interval)`

---

### BUG-019｜回放結束績效報告完全硬編碼
- **模組**：M6 歷史回放
- **現象**：無論實際操作為何，結束後永遠顯示「總報酬：+3.2%、勝率：67%、紀律評分：85/100」
- **根因**：`endReplay()` Alert 訊息為靜態字串，未計算 `replayState` 中的實際交易記錄

---

### BUG-020｜M1 總未實現損益與持倉資料嚴重不符
- **模組**：M1 戰情中心
- **現象**：顯示「+$124,800」，但依 `holdingsData` 計算實際損益為 **+340**（差距達 **124,460**）
- **根因**：數值硬編碼，未從 `holdingsData` 動態計算。`shares` 欄位單位（是「張」還是「股」）定義不明亦造成計算混亂

---

### BUG-021｜策略卡「onchange」與「載入」按鈕重複觸發
- **模組**：M4 交易策略室
- **重現**：從 `<select>` 下拉選單直接選擇後，再點「載入策略卡」按鈕
- **現象**：`loadStrategyCard()` 被觸發兩次（onchange 一次 + 按鈕一次），效能浪費
- **建議**：擇一保留，或讓按鈕顯示選單當前值

---

## 🔵 LOW（4 項）

### BUG-022｜月度熱圖月份顯示英文
- **模組**：M5 回測實驗室
- **現象**：熱圖標題顯示 Jan、Feb、Mar...，其餘 UI 全為繁體中文
- **修正方向**：改為「1月、2月...」或「01–12」

---

### BUG-023｜今日焦點卡點擊無反應
- **模組**：M1 戰情中心
- **現象**：焦點卡有 `cursor:pointer` 樣式，點擊後無任何動作（應展開詳細或跳轉）

---

### BUG-024｜「市場開盤中」永遠顯示，非交易時間不更新
- **模組**：全域 Navbar
- **現象**：無論現在幾點，Navbar 永遠顯示綠燈「市場開盤中」
- **建議**：根據當前時間（09:00–13:30）動態切換「開盤中 / 收盤中 / 休市」

---

### BUG-025｜全球發表追蹤表格：小螢幕無水平滾動
- **模組**：M2 全球情報站
- **現象**：`<table>` 無 `overflow-x:auto` 外層容器，在窄螢幕下欄位會直接裁切或溢出破版

---

## 統計摘要

| 等級 | 數量 |
|------|------|
| 🔴 CRITICAL | 4 |
| 🟠 HIGH | 8 |
| 🟡 MEDIUM | 9 |
| 🔵 LOW | 4 |
| **合計** | **25** |

---

## 建議修正優先順序

1. **立即修復**：BUG-001（產業鏈切換）、BUG-002（回放時間軸）、BUG-004（K棒圖）
2. **本版修復**：BUG-005/006（Chart重複實例）、BUG-007（止損邏輯）、BUG-008（樣式污染）、BUG-009（K棒不更新）
3. **下版跟進**：MEDIUM 級別（BUG-013 ~ BUG-021）
4. **優化補強**：LOW 級別（BUG-022 ~ BUG-025）
